apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations:
    kubernetes.io/description: |
      This ClusterRole grants read/write access to kro resources by aggregating into the admin and edit ClusterRoles.
  name: {{ include "kro.fullname" . }}-edit
  labels:
    {{- include "kro.labels" . | nindent 4 }}
    rbac.authorization.k8s.io/aggregate-to-edit: "true"
    rbac.authorization.k8s.io/aggregate-to-admin: "true"
rules:
- apiGroups:
  - kro.run
  # "*" is needed so RGD-generated kinds (default group kro.run) stay covered;
  # it also grants graphs.kro.run when that CRD is installed.
  resources: ["*"]
  verbs:
  - create
  - delete
  - deletecollection
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations:
    kubernetes.io/description: |
      This ClusterRole grants read access to kro resources by aggregating into the view, edit, and admin ClusterRoles.
  name: {{ include "kro.fullname" . }}-view
  labels:
    {{- include "kro.labels" . | nindent 4 }}
    rbac.authorization.k8s.io/aggregate-to-admin: "true"
    rbac.authorization.k8s.io/aggregate-to-edit: "true"
    rbac.authorization.k8s.io/aggregate-to-view: "true"
rules:
- apiGroups:
  - kro.run
  # "*" for the same reason as in the -edit role above.
  resources: ["*"]
  verbs:
  - get
  - list
  - watch
